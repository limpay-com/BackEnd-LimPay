# application.properties

# Configura��o do banco de dados H2 (exemplo)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Configura��es espec�ficas do JWT (exemplo)
jwt.secret=secretpassword
jwt.expiration=3600

# Configura��es do envio de e-mail
spring.mail.host=meu-smtp-server
spring.mail.port=587
spring.mail.username=your-email@gmail.com
spring.mail.password=your-email-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configura��es adicionais
spring.mail.properties.mail.smtp.ssl.trust=your-smtp-server

